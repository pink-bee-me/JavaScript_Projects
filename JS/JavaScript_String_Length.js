function Working_That_String() {
    let str = "How many characters make up this example?";
    let rtn = str.length;
    document.getElementById("Example_Return").innerHTML = rtn + " is the number of characters in that example string.";
}